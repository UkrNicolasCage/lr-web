export const USER_KEYS = Object.freeze({
    USERS: 'users',
    ME: 'me',
})

export const AUTH_KEYS = Object.freeze({
    AUTH: 'auth',
    CHECK: '/check',
    SIGNUP: '/signup',
    SIGNIN: '/signin',
    LOGOUT: '/logout',
})

export const STATIONS_KEYS = Object.freeze({
    STATIONS: 'stations',
    ONE: '/:id',
})

export const SCHEDULE_KEYS = Object.freeze({
    SCHEDULES: 'schedules',
    ONE: '/:id',
})